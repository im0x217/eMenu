services:
  - type: web
    name: e-menu
    env: node
    plan: free
    buildCommand: npm install
    preDeployCommand: node migrate_to_cloudinary.js
    startCommand: node server.js
    envVars:
      - fromGroup: e-menu-secrets
